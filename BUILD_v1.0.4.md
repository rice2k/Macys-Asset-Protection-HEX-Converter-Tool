# Build AP HEX Converter Tool v1.0.4

The GitHub Actions workflow applies the reviewed v1.0.4 source patch, generates the updated banner assets, runs syntax and converter self-tests, and builds the Windows executable with PyInstaller.

Expected output: `AP_HEX_Converter_Tool_v1.0.4.exe`.
