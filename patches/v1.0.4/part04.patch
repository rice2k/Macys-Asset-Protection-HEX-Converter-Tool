+            self.results_popout.destroy()
+        self.results_popout = None
+        self.results_popout_tree = None
+        self.results_popout_lookup = {}
+
+    def refresh_results_popout(self) -> None:
+        tree = self.results_popout_tree
+        if tree is None or self.results_popout is None or not self.results_popout.winfo_exists():
+            return
+        for item in tree.get_children():
+            tree.delete(item)
+        self.results_popout_lookup.clear()
+        query = self.results_popout_search_var.get().strip().lower() if hasattr(self, "results_popout_search_var") else ""
+        status_filter = self.results_popout_status_var.get() if hasattr(self, "results_popout_status_var") else "All"
+        combined: list[tuple[int, str, ConvertedRow | InvalidRow]] = [(row.line, "valid", row) for row in self.results] + [(row.line, "invalid", row) for row in self.invalid]
+        shown = 0
+        for _line, kind, row in sorted(combined, key=lambda item: item[0]):
+            if kind == "invalid":
+                status = "Invalid"
+                values = (row.line, row.raw, "", "", status, row.reason)
+                tags = ("invalid",)
+            else:
+                status = "Warning" if row.warnings else "Valid"
+                notes = " | ".join([*row.suggestions, *row.warnings])
+                values = (row.line, row.hex_value, row.facility, row.card, status, notes)
+                tags = ("warning",) if row.warnings else ()
+            if status_filter != "All" and status != status_filter:
+                continue
+            if query and query not in " ".join(str(value) for value in values).lower():
+                continue
+            item_id = tree.insert("", "end", values=values, tags=tags)
+            self.results_popout_lookup[item_id] = row
+            shown += 1
+        if shown == 0:
+            message = "No matching results." if combined else "No results yet. Import IDs and use Convert All."
+            tree.insert("", "end", values=("", message, "", "", "", ""), tags=("empty",))
+        if hasattr(self, "results_popout_count_var"):
+            self.results_popout_count_var.set(f"Showing {shown} of {len(combined)} row(s)")
+
+    def copy_popout_selected(self, mode: str) -> None:
+        tree = self.results_popout_tree
+        if tree is None:
+            return
+        selected = tree.selection()
+        if not selected:
+            messagebox.showinfo("Select a result", "Select a valid result row first.", parent=self.results_popout)
+            return
+        row = self.results_popout_lookup.get(selected[0])
+        if row is None or isinstance(row, InvalidRow):
+            messagebox.showinfo("Invalid row", "The selected row does not contain a valid FC/CN result.", parent=self.results_popout)
+            return
+        values = {
+            "hex": row.hex_value,
+            "fc": str(row.facility),
+            "cn": str(row.card),
+            "pair": f"{row.facility},{row.card}",
+        }
+        self.clipboard_clear()
+        self.clipboard_append(values.get(mode, values["pair"]))
+        self.set_status("Selected result copied from the full results window.")
+
     def sort_results_by(self, column: str) -> None:
         if self.sort_column == column:
             self.sort_reverse = not self.sort_reverse
@@ -2340,6 +2605,7 @@
         self.stat_vars["Warnings"].set(str(warn_count))
         self.time_var.set(f"Converted {self.last_converted_at}" if self.last_converted_at else "No run yet")
         self.notice_var.set(f"{warn_count} warning(s) | {len(self.invalid)} invalid line(s)" if self.results or self.invalid else "")
+        self.refresh_results_popout()
 
     def render_unconvert_results(self) -> None:
         for item in self.unconvert_tree.get_children():
@@ -2478,7 +2744,7 @@
         tk.Label(export_inner, text="Default export type", bg=UI_SURFACE, fg=UI_MUTED, font=("Segoe UI", 9, "bold")).pack(anchor="w", pady=(0, 4))
         type_menu_button = tk.Menubutton(
             export_inner,
-            text=f"{export_type_var.get()} v",
+            text=f"{export_type_var.get()}  ▾",
             bg=UI_INPUT,
             fg=UI_TEXT,
             activebackground="#eef2f6",
@@ -2497,7 +2763,7 @@
 
         def choose_export_type(value: str) -> None:
             export_type_var.set(value)
-            type_menu_button.configure(text=f"{value} v")
+            type_menu_button.configure(text=f"{value}  ▾")
 
         for choice in EXPORT_TYPE_CHOICES:
             type_menu.add_command(label=choice, command=lambda value=choice: choose_export_type(value))
@@ -2561,15 +2827,19 @@
     def app_launch_target(self) -> Path:
         if getattr(sys, "frozen", False):
             return Path(sys.executable)
-        built_exe = Path(__file__).resolve().parent / "dist" / "Macys_AP_China_Grove_Hex_Utility.exe"
-        return built_exe if built_exe.exists() else Path(sys.executable)
+        dist_dir = Path(__file__).resolve().parent / "dist"
+        versioned_exe = dist_dir / f"AP_HEX_Converter_Tool_v{APP_VERSION}.exe"
+        legacy_exe = dist_dir / "AP_HEX_Converter_Tool.exe"
+        if versioned_exe.exists():
