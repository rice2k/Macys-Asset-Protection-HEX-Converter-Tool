             font=("Cascadia Mono", 11),
-            wrap="none",
+            wrap="word",
+            spacing1=2,
+            spacing3=2,
         )
         self.multi_text.tag_configure("input_valid", background=UI_GREEN_SOFT, foreground=UI_GREEN_TEXT)
         self.multi_text.tag_configure("input_warning", background=UI_WARN_SOFT, foreground=UI_WARN_TEXT)
         self.multi_text.tag_configure("input_invalid", background=UI_BAD_SOFT, foreground=UI_BAD_TEXT)
         multi_scroll_y = ttk.Scrollbar(multi_frame, orient="vertical", command=self.multi_text.yview, style="Dark.Vertical.TScrollbar")
-        multi_scroll_x = ttk.Scrollbar(multi_frame, orient="horizontal", command=self.multi_text.xview, style="Dark.Horizontal.TScrollbar")
-        self.multi_text.configure(yscrollcommand=multi_scroll_y.set, xscrollcommand=multi_scroll_x.set)
+        self.multi_text.configure(yscrollcommand=multi_scroll_y.set)
         self.multi_text.grid(row=0, column=0, sticky="nsew")
         multi_scroll_y.grid(row=0, column=1, sticky="ns")
-        multi_scroll_x.grid(row=1, column=0, sticky="ew")
         self.multi_text.insert("1.0", "")
         self.multi_text.bind("<<Modified>>", self.handle_batch_input_changed)
         self._enable_mousewheel(self.multi_text, self.multi_text, self.multi_text)
@@ -1612,6 +1681,7 @@
         tk.Label(header, text="Results", bg="#10141b", fg="#ffffff", font=("Segoe UI", 12, "bold")).pack(side="left")
         copy_row = tk.Frame(header, bg="#10141b")
         copy_row.pack(side="right")
+        self._button(copy_row, "Pop Out Results", self.show_results_popout, icon="icon-folder.png", tooltip="Open the complete output in a separate full-size results window.").pack(side="left", padx=(0, 6))
         self._button(copy_row, "Copy All", self.copy_all_pairs, icon="icon-copy.png", tooltip="Copy all valid FC,CN pairs to the clipboard.").pack(side="left", padx=(0, 6))
         self._button(copy_row, "Clear Invalid", self.clear_invalid_rows, icon="icon-clear.png", tooltip="Remove invalid rows from the review table.").pack(side="left", padx=(0, 6))
         self._button(copy_row, "Copy FC", lambda: self.copy_selected("fc"), icon="icon-copy.png", tooltip="Copy the selected row's Facility Code.").pack(side="left", padx=(0, 6))
@@ -1960,11 +2030,47 @@
         if index == 4:
             self.render_history()
 
-    def set_status(self, message: str) -> None:
+    def set_status(self, message: str, link_path: Path | str | None = None) -> None:
         self.status_var.set(message)
         if hasattr(self, "nav_status"):
             self.nav_status.set(message)
 
+        self.status_link_path = Path(link_path) if link_path else None
+        is_link = self.status_link_path is not None
+        cursor = "hand2" if is_link else "arrow"
+        nav_font = ("Segoe UI", 9, "bold underline") if is_link else ("Segoe UI", 9, "bold")
+        footer_font = ("Segoe UI", 9, "underline") if is_link else ("Segoe UI", 9)
+
+        if hasattr(self, "nav_status_label"):
+            self.nav_status_label.configure(cursor=cursor, font=nav_font, fg=UI_BLUE)
+            self.nav_status_label.unbind("<Button-1>")
+            self.nav_status_label.unbind("<Enter>")
+            self.nav_status_label.unbind("<Leave>")
+            if is_link:
+                self.nav_status_label.bind("<Button-1>", self._open_status_link)
+                self.nav_status_label.bind("<Enter>", lambda _event: self.nav_status_label.configure(fg=UI_RED))
+                self.nav_status_label.bind("<Leave>", lambda _event: self.nav_status_label.configure(fg=UI_BLUE))
+
+        if hasattr(self, "footer_status_label"):
+            self.footer_status_label.configure(cursor=cursor, font=footer_font, fg=UI_BLUE if is_link else UI_MUTED)
+            self.footer_status_label.unbind("<Button-1>")
+            self.footer_status_label.unbind("<Enter>")
+            self.footer_status_label.unbind("<Leave>")
+            if is_link:
+                self.footer_status_label.bind("<Button-1>", self._open_status_link)
+                self.footer_status_label.bind("<Enter>", lambda _event: self.footer_status_label.configure(fg=UI_RED))
+                self.footer_status_label.bind("<Leave>", lambda _event: self.footer_status_label.configure(fg=UI_BLUE))
+
+        if hasattr(self, "nav_status_tooltip"):
+            if is_link:
+                self.nav_status_tooltip.text = f"Click to open the saved output file:\n{self.status_link_path}"
+            else:
+                self.nav_status_tooltip.text = "Shows the latest action or result from the current workspace."
+
+    def _open_status_link(self, _event: Any | None = None) -> None:
+        if self.status_link_path:
+            self.open_path(self.status_link_path, "file")
+
     def _enable_drop_target(self, widget: tk.Widget) -> bool:
         """Register a TkDND target and report whether registration succeeded."""
         if DND_FILES is None or not hasattr(widget, "drop_target_register"):
@@ -2213,17 +2319,17 @@
 
     def load_sample(self) -> None:
         self.multi_text.delete("1.0", "end")
-        self.multi_text.insert("1.0", "88984717\n88984130\nActive Christopher Benson, 88984765\nBAD-LINE")
+        self.multi_text.insert("1.0", "88984717\n88984130\n88984765\n88984A20")
         self.multi_text.edit_modified(True)
         self.handle_batch_input_changed()
-        self.set_status("Sample IDs loaded.")
+        self.set_status("Working HEX examples loaded. All sample rows are valid.")
 
     def load_unconvert_sample(self) -> None:
         self.unconvert_text.delete("1.0", "end")
-        self.unconvert_text.insert("1.0", "34968,18199\nFC 34968 CN 18192\n34968\t18277\nBAD-LINE")
+        self.unconvert_text.insert("1.0", "34968,18199\n34968,16688\n34968,18277\n34968,18976")
         self.unconvert_text.edit_modified(True)
         self.handle_unconvert_input_changed()
-        self.set_status("Unconvert sample loaded.")
+        self.set_status("Working FC/CN examples loaded. All sample rows are valid.")
 
     def clear_workspace(self) -> None:
         self.multi_text.delete("1.0", "end")
@@ -2278,6 +2384,165 @@
         self.render_unconvert_results()
