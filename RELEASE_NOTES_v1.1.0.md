# Macy's AP China Grove Hex Utility v1.1.0

## Highlights

- Adds a cleaned clipboard preview for messy Excel-style copied data before it enters the Input Queue.
- Places structured Excel/CSV imports into the queue as clean numeric ID rows.
- Adds a live row count to the Input Queue.
- Adds Copy Row and right-click copy menus for Results and Unconvert Results.
- Adds a small import working indicator while files are being read.
- Adds a Settings action to clear saved Recent Exports shortcuts.
- Keeps v1.0.9 as the almost-done baseline restore point.

## Verification

- Syntax check
- Desktop smoke checks
- Built-in self-test
- Windows EXE build with checksum

## Restore Notes

- Previous restore tag: `v1.0.9`
- Previous archive branch: `archive/v1.0.9`
- This release should use the matching EXE and SHA-256 checksum generated from `dist`.
